import { HeaderContainer } from "./style";

/* TEST: first component of the project */
export const Header = ( props ) => {
    return (
        <HeaderContainer>
            <h1> Olá Mundo </h1>
        </HeaderContainer>
    )
}